import React from 'react';

const Header: React.FC = () => {
  return (
    <header style={{ padding: '1rem', backgroundColor: '#eee' }}>
      <h1>Comparatore Tipi di Carne</h1>
    </header>
  );
};

export default Header;
